## Claude.md

此文件用于向 AI 助手（Claude）提供项目上下文，确保后续协作中准确理解《聚变纪元·启元一号》的设计意图、技术边界与当前进展。

---

# 项目概述

**项目名称**：聚变纪元·启元一号  
**定位**：基于 Godot 4.6 的纯单机离线科普游戏，体验"托卡马克装置中控室紧急排障"  
**目标用户**：高校本科生、研究生（理工/文科均可），也适配中小学科普与展馆互动  
**核心承诺**：零服务器、零网络请求、完全离线，下载即玩，深度科普可控核聚变原理与工程现实

---

# 技术栈与约束

- **引擎**：Godot 4.6（Forward+ 渲染器）
- **语言**：GDScript（100%）
- **图形**：2D/2.5D 中控台场景 + GPUParticles2D 粒子特效 + Node2D `_draw` 程序矢量
- **数据**：所有科普文本、故障逻辑、专家台词、结局文案存储在 `res://data/` 下的 **JSON 配置文件**中，由游戏逻辑动态读取
- **存储**：Godot `ConfigFile`（存档、设置、成就）
- **导出目标**：Windows (.exe)、macOS (.app)、Linux 二进制、HTML5（纯静态网页）
- **部署**：桌面端直接分发；网页端上传至 GitHub Pages 等静态托管，完全离线可用
- **禁止依赖**：**绝对不能引入任何网络请求、第三方 SDK、云数据库、用户认证系统**，所有资源 100% 本地
- **源码编码**：所有 `.gd/.tscn/.json` 必须 **UTF-8 无 BOM + LF**——Godot 4.6 对脚本/场景文件头部 BOM（U+FEFF）零容忍，会致解析失败、编辑器 debug 直接崩。已由 `.editorconfig`（`charset=utf-8`）+ `.gitattributes`（`eol=lf`）约束，编辑器需启用 EditorConfig。

---

# 核心玩法设计（V3.1 当前方案）

**体验核心**：玩家扮演"启元一号"当班运行总工程师，面对四轮递进式耦合故障，在有限经费、时间和信息不完全透明的压力下，通过分析仪表数据、拼合四个子系统负责人的信息片段，分配维护预算，找出故障根源，维持等离子体运行。

### 四大子系统负责人（信任度驱动的性格化专家）

> **重要**：V3.1 起专家不再"说谎"。改为**信任度→情报精度**模型：
> - 信任度 ≥ 0.5 → 输出**精确简报**（窄区间 / 确定语气）
> - 信任度 < 0.5 → 输出**模糊简报**（宽区间 / 含糊措辞）
> - 每位专家只对 `direct_gauge`（自身负责仪表）有精确直接数据，对他人部位只能间接推断
> - 信任度变化规则：玩家在该专家负责部位对症投入 → 信任 +；完全无视其警告 → 信任 −

1. **磁体工程师**（老派稳妥）：负责环向场，`direct_gauge=toroidal_field`
2. **材料与第一壁负责人**（激进冒险）：负责第一壁，`direct_gauge=wall_temp`
3. **氚循环工程师**（悲观预警）：负责氚增殖包层，`direct_gauge=tritium_ratio`
4. **等离子体物理师**（理想主义）：负责等离子体控制，`direct_gauge=plasma_temp`

### 经费分配与博弈机制

- **拖放式资源分配**：经费代币拖到装置四部位，边际递减效果公式；可撤回。
- **锁定预览（T3.1，决策 B 已执行）**：已彻底移除实时悬停假想增量预测；分配期仅回显"已投入实际效果"（健康条 + 落定后实际 Q/稳定/燃料），点提交后弹一次性**锁定预览**（结果 + 倒计时逾期自动确认），决策锁定不可反复试探。`FusionEngine.predict_with_extra` 仅保留供结算与锁定预览（`extra=""`）。
- **超额投入奖励**：同一部位投入 ≥ 50 单位 → 下一轮该部位关联根因故障强度 ×(1−5%)，仪表显示"✓稳定"角标。不跨轮累积。
- **深度诊断**：消耗 ¥10 经费，可让某专家给出其负责仪表近三轮趋势 + 语气解读（不判定故障真假）。
- **逻辑一致性提示**：提交前检测分配方向与最强仪表表象明显矛盾时，弹一次温和提示（不评判，可继续）。
- **复盘三图（T3.3）**：结算后可调出分配比例 / 故障强度变化 / 仪表趋势对比图，无"最优方案"标注，纯视觉教学。

### 故障与耦合设计

- **故障树**：三个根本原因（磁体电源老化 / 第一壁微裂纹 / 氚提取泵效率下降）→ 四轮递进表象。当前剧本可限定本局激活的根因子集（见手作剧本）。
- **故障链**：主因未修复时次因仪表产生同步特征，显示"⇌链路"角标。现有两条链：`magnet_to_wall_hotspot`（磁体约束弱化 → 第一壁热斑）、`tritium_magnet_constraint`（氚提取衰退 → 磁体约束受限）；启用哪条、触发轮由当前剧本声明（无剧本/教学回退 `faults.json` 全部链 + 各自 trigger_round）。
- **手作剧本随机（T3.2）**：`data/scenarios.json` 5 套，`GameState.reset` 开局随机抽取一套。剧本驱动当局：① 激活根因子集（`FaultTree.round_symptoms` 收口过滤）；② `gauge_offset` 仪表基准偏移；③ `chains`+`trigger_round` 故障链组合；④ `expert_roles` 专家立场（`observer` 恒模糊 / `secondary` 阈值上浮 `secondary_precise_penalty` / `primary` 同基础阈值）。专家**人设固定、角色由剧本指派**；所辖根因本局不激活的专家给中性简报（`briefings._neutral`），不陈述不存在的故障。无剧本/教学全回退 V3.0。
- **不可逆损伤**：某部位有故障却长期投入不足 → `irreversible_damage` 累积 → 后续轮该部位故障强度放大，形成真实内生后果。
- **稳定度历史**：连续两轮稳定度下降 → 提交反馈中显示内生风险警告。
- **结局矩阵**：Q 值区间(3) × 根因识别数(0-3) = 12 种结局，每种给出科普总结；另含 2 个隐藏结局 `hidden_meltdown`（连锁熔毁）/ `hidden_flawless`（完美一班），由系统状态自然触发、优先于矩阵（`EndingResolver._check_hidden`）。

### 成就系统

条件型行为成就（`data/achievements.json`），庆祝多样性玩法而非唯一最优解：

| ID | 名称 | 触发条件 |
|----|------|---------|
| `deaf_ear` | 偏听则暗 | 某专家部位全程零投入，仍Q≥1点火 |
| `chain_hunter` | 耦合猎手 | 某一轮一次识破≥2根因 |
| `phoenix` | 烈火中永生 | 稳定度曾跌破破裂阈值，最终回升 |
| `budget_madman` | 预算狂人 | 总花费<标准线80%且Q≥1通关 |

### 教学战役（T3.3）

三章渐进解锁教学流程，独立进度保存于 `user://tutorial.cfg`，与标准模式成就/存档互不污染：

| 章节 | 可用部位 | 可用专家 | 轮次 | 核心教学点 |
|------|---------|---------|------|-----------|
| 第一章：磁场与第一壁 | magnet_coil, first_wall | magnet_eng, wall_eng | 3 | 阅读仪表、专家简报、预算分配 |
| 第二章：氚燃料循环 | + breeder_blanket | + tritium_eng | 4 | 三部权衡、专家信任度博弈 |
| 第三章：全系统综合 | 全部4部位 | 全部4专家 | 4 | 故障链耦合、终局决策 |

### 难度模式

| 模式 | 限时 | 噪声类型 | 简报精度提示 | 建议提示 | 历史日志 |
|------|------|----------|------------|----------|----------|
| **新手/科普** | 不限时 | 无 | ✓ 精确/模糊标签 | ✓ 角标 | — |
| **总工** | 300s | 确定性低频正弦 | — | — | ✓ |
| **自定义** | 240s | 正弦（较弱） | — | — | ✓ |
| **挑战** | 180s | 正弦（强） | — | — | ✓ |

> 噪声改为**确定性低频正弦**（`amp*sin(round*freq+phase)`），多轮观察即可识别周期，从而与真实异常区分（区别于旧版随机噪声）。

---

# 科普植入原则

- **零弹窗打断**：科普信息"长"在操作对象上——投资时浮动标签（3秒自动消退，可钉住）；报警时点击展开原理。
- **内置运行手册**（H键）：全本地检索，含科普词条、中国聚变成果时间线、辟谣、考研就业指南。
- **仪表历史趋势图**：历史日志面板（L键）含跨轮折线图，识别渐变与突发故障。
- **可视化即科普**：等离子体色彩对应稳定度，故障链角标，破裂报警动画。

---

# 重要设计决策与边界

1. **严格离线**：不得出现任何"检查更新""在线排行榜""分享到社交网络"等依赖网络的功能。
2. **配置化驱动**：所有文本与数值外置 JSON，非开发人员可直接修改；`DataManager` 启动时加载并校验，缺字段明确报错。
3. **专家不说谎**：V3.1 起专家信息模型不含"隐瞒/夸大"逻辑；精度随信任度变化，但均为真实情况的不同粒度描述。如需修改此行为，须显式告知。
4. **向下兼容原则**：Phase 2+ 的所有改动不破坏四轮核心循环。剧本系统与教学走"覆盖 + 回退"：`scenario_data` 为空（教学模式或无剧本）时全部回退 V3.0 逻辑；标准模式开局必抽一套剧本，故各局有差异（`novice` 体验在剧本框架内，与 V3.0 同源但不再逐帧等同）。
5. **决策 B 已执行（锁定预览）**：实时悬停假想增量预测已彻底移除（`DropZone.hover_preview` 信号 + `ControlRoom._on_hover_preview`/`_show_prediction`/`_predicting` 删除），保留"已投入实际效果回显"。如需回退此交互须显式告知。
6. **剧本系统消费边界（T3.2）**：已机械消费 `root_causes`/`gauge_offset`/`chains`/`expert_roles` 与隐藏结局；`briefings_override` 仅留**架构钩子**（按 `scenarios[].briefings_override[round][expert].{precise,vague}` 填专属台词，缺省回退基础表，属可选内容创作）；`root_logic`（`round_distribution` 等）为**描述性元数据**，未做"按轮动态生成症状"的机械消费。如需让剧本进一步差异化（专属台词 / 动态症状分布），在此基础上扩展。

---

# 项目当前状态（2026-06-15）

**V3.0** 已完整交付（`win/`、`h5/` 导出产物 + GitHub Pages 在线）。

**V3.1 增量优化进度**：

- [x] **Phase 1（纯增量）**：仪表历史趋势折线图、确定性周期噪声、超额投入奖励、行为成就系统、逻辑一致性提示
- [x] **Phase 2（信息系统重构）**：专家信任度→精度模型、深度诊断趋势报告、故障链耦合可读化、内生后果（不可逆损伤+稳定历史）、挑战难度模式
- [x] **Phase 3（交互范式与可重玩）**：锁定预览取代实时悬停试探（T3.1）、手作剧本随机抽取 + 隐藏结局（T3.2，剧本消费已接入；`briefings_override` 专属台词为可选扩展）、复盘三图 + 教学战役三章（T3.3）
  - 后续可选深化：剧本专属台词集、`root_logic` 按轮动态症状分布、复盘对比的更多维度。

---

# 给 AI 助手的协作指南

- 当询问关于本项目的问题时，请严格在上述技术栈、玩法设计和离线约束下作答。
- 优先给出**可落地的 GDScript 代码片段、JSON 结构示例、场景树设计建议**。
- 科普文本的建议请保持科学严谨性，参考 EAST、ITER 等真实装置数据，避免未来化幻想。
- 任何偏离"纯单机离线""Godot 引擎""配置化驱动"的提议，请先征询是否突破约束。
- **V3.1 专家模型已改为信任度→精度，不再隐瞒/夸大**；修改专家行为逻辑前须确认不回退此决策。

*本文件随项目推进持续更新。参考 `plan.md`（V3.1 增量工程计划）、`new.md`（V3.1 优化方案原文）。*

